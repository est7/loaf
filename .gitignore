# local planning notes (not shipped). The session trio (task_plan /
# progress / findings) was consolidated into backlog.md at v0.1.1.
/backlog.md

# dependencies (bun install)
node_modules

# output
out
dist/*
!dist/cli.mjs
*.tgz

# code coverage
coverage
*.lcov

# logs
logs
_.log
report.[0-9]_.[0-9]_.[0-9]_.[0-9]_.json

# dotenv environment variable files
.env
.env.development.local
.env.test.local
.env.production.local
.env.local

# caches
.eslintcache
.cache
*.tsbuildinfo

# IntelliJ based IDEs
.idea

# Finder (MacOS) folder config
.DS_Store

# Agent Message Queue
.agent-mail/
.amqrc
implementation-notes.md

# Phase 16 SC-6b — `--debug` observability trace files (Debug-trace per
# protocol §13.1; local debug data, never committed).
**/.loaf/*/trace.jsonl
