{
  "name": "loaf-cli",
  "version": "0.7.0",
  "description": "Protocol kernel for the loaf feature-lifecycle workflow — single typed journal + in-memory projections.",
  "type": "module",
  "bin": {
    "loaf": "./dist/cli.mjs"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "engines": {
    "node": ">=22",
    "bun": ">=1.0.0"
  },
  "scripts": {
    "dev": "bun run src/cli.tsx",
    "build": "tsdown",
    "gen:fsm": "bun run scripts/gen-fsm-artifacts.ts",
    "gen:i18n": "bun run scripts/gen-i18n-diagnostics.ts",
    "gen:errors": "bun run scripts/gen-error-catalog.ts",
    "verify:codegen": "bun run scripts/gen-fsm-artifacts.ts --check && bun run scripts/gen-i18n-diagnostics.ts --check && bun run scripts/gen-error-catalog.ts --check",
    "verify:release-identity": "bash scripts/public-contract-version-check.sh",
    "typecheck": "tsc --noEmit",
    "lint": "biome check --formatter-enabled=false --assist-enabled=false src tests tsdown.config.ts package.json tsconfig.json biome.json",
    "format": "biome format --write src tests tsdown.config.ts package.json tsconfig.json biome.json",
    "test": "vitest run",
    "check": "bun run lint && bun run typecheck && bun run test && bun run build",
    "ga:pack-smoke": "bash scripts/ga-package-smoke.sh",
    "ga:consistency": "bash scripts/ga-consistency-check.sh",
    "ga:check": "bun run build && bun run ga:pack-smoke && bun run ga:consistency",
    "inventory": "bash scripts/cli-inventory.sh",
    "prepublishOnly": "bun run check"
  },
  "packageManager": "bun@1.3.13",
  "license": "MIT",
  "dependencies": {
    "commander": "^14.0.3",
    "ink": "^7.0.2",
    "picomatch": "^4.0.4",
    "react": "^19.2.6",
    "yaml": "^2.9.0",
    "zod": "^4.4.3"
  },
  "overrides": {
    "ws": "^8.21.0"
  },
  "devDependencies": {
    "@biomejs/biome": "2.4.16",
    "@types/node": "^25.7.0",
    "@types/picomatch": "^4.0.3",
    "@types/react": "^19.2.14",
    "tsdown": "^0.22.0",
    "typescript": "^6.0.3",
    "vitest": "^4.1.6"
  }
}
